# Ether Hunt — local env example
# Copy to services/audit-api/.env or export in your shell.

PORT=8787

# Local only — set to false for Hedera prize demos
DEV_BYPASS_PAYMENT=true

# The Graph (required for prize-eligible live evidence)
# GRAPH_SUBGRAPH_URL=https://gateway.thegraph.com/api/<KEY>/subgraphs/id/<ID>
# GRAPH_API_KEY=
